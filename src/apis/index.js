import Request from '../network/request';
import { CONFIG } from '../config';

const url = `${CONFIG.URL}/system-server`;
// /**
// * 检测APP版本更新
// */
// export const getAppUpdateService = (params = {}) => Request.get(`${url}/phoneApi/sms/version/update/${params.version}/${params.clientType}`);
