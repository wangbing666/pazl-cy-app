import { StyleSheet } from 'react-native';
import { ColorCommon } from './publicConsts';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: ColorCommon.bgGray
  }
});
